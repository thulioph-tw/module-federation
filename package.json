{
  "name": "module-federation",
  "version": "1.0.0",
  "description": "A proof of concept to test module federation",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start": "wsrun --parallel start",
    "dev": "wsrun --parallel dev",
    "build": "yarn workspaces run build",
    "clean": "rm -rf node_modules apps/**/node_modules"
  },
  "author": "thulioph",
  "license": "ISC",
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}